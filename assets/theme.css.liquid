/* general */


/* $orange: #ffc13d; */
/* $navy: #2c3485; */
/* $blue: #2f55a6; */
/* $gray: #fcfaf7; */

.ttu { text-transform: uppercase; }
.ib { display: inline-block; }
.nm { margin: 0; }
.nmt { margin-top: 0; }
.nmb { margin-bottom: 0; }
body { 
  margin: 0;
  color: black;
}
html { padding-bottom: 0 !important; }
.tar { text-align: right; }
.fw { width: 100%; }
.flex-container-no{
  display: flex;
  align-items: center;
}
@media only screen and (min-width: 589px) {
  .flex-container {
    display: flex;
    align-items: center;
  }
  .spacer-right { margin-right: 20px; }
  .spacer-left { margin-left: 20px; }
  .accent-padding { padding: 50px; }
  .half { width: 50%;}
  #press-quote {margin: 20px auto;}
  #product-detail-image, #atf { height: 500px;}
}
@media only screen and (max-width: 588px) {
  .accent-padding { padding: 20px; }
  .half { width: 100%; }
  .highlight { margin: 20px; }
  #atf { height: 350px;}
  #product-detail-image { height: 200px;}
}
.tac { text-align: center; }
.subscript {font-size: 8pt !important;}
.half { box-sizing: border-box;}
.two-thirds { width: 66%; }
.quarter { 
  width: 25%; 
  box-sizing: border-box;
}
.three-fourths { width: 75%; }
.third { width: 33%; }
.fe { align-self: flex-end; }
.jes { justify-content: space-evenly; }
.jcfs { justify-content: flex-start;  }
.jcc { justify-content: center; }
.ma { margin: 0 auto; }
.jcfe { justify-content: flex-end; }
.jcc { justify-content: center; }
.aifs { align-items: flex-start; }
.text-white { color: white; }
.text-navy { color: navy; }
.background-lightgray { background-color: lightgray; }
.background-gray { background-color: gray; }
.background-navy { background-color: navy; }
.button { 
  width: 200px;
  text-align: center;
  padding: 10px;
}
h1,h2,h3,h4,h5,a, span, th, select { 
  font-family: futura-pt-bold, sans-serif;
  font-weight: 700;
  font-style: normal;
}
p, textarea, td, ul, li, .error_message, .success_message, input { 
  font-family: futura-pt, sans-serif;
  font-weight: 400;
  font-style: normal;
} 
a { text-decoration: none; color: black;}
@media only screen and (min-width: 589px) {

#live-feed-wrapper {
    padding: 50px 50px 50px 45px;
}
}
@media only screen and (max-width: 588px) {
  .flex-container { display: block; }
  .flex-container .half { width: 100%; }
  #page-logo {max-width: 75px;}

#live-feed-wrapper {
    padding: 30px 30px 30px 15px;
}


  #cart { margin-top: -3.5px; }
}


/* header */
nav p { 
  color:#2c3485;
  margin: 20px;
}
#cart { width: 45px; }
#hamburger {
  display: inline-block; 
  width: 40px;
  -webkit-transform: rotate(0deg);
  -moz-transform: rotate(0deg);
  -o-transform: rotate(0deg);
  transform: rotate(0deg);
  -webkit-transition: .5s ease-in-out;
  -moz-transition: .5s ease-in-out;
  -o-transition: .5s ease-in-out;
  transition: .5s ease-in-out;
  cursor: pointer;
}
#hamburger span {
  display: block;
  height: 3px;
  margin: 7px;
  width: 100%;
  background: #2c3485;
  border-radius: 9px;
  opacity: 1;
  left: 0;
  -webkit-transform: rotate(0deg);
  -moz-transform: rotate(0deg);
  -o-transform: rotate(0deg);
  transform: rotate(0deg);
  -webkit-transition: .25s ease-in-out;
  -moz-transition: .25s ease-in-out;
  -o-transition: .25s ease-in-out;
  transition: .25s ease-in-out;
}

/* live feed */
.feed-row {
  display: flex;
  flex-wrap: wrap;
  padding: 0 10px;
}

.feed-column {
  flex: 25%;
  max-width: 25%;
  padding: 10px;
  box-sizing: border-box;
}

.feed-column div {
  margin-top: 10px;
  vertical-align: middle;
}
.feed-column div img { display: block; margin: 0 auto; }
@media screen and (max-width: 799px) {
  .feed-column {
    flex: 50%;
    max-width: 50%;
  }
}
.left-arrow { transform: rotate(-90deg); }
.right-arrow { transform: rotate(90deg); }
#live-feed h4 { padding-top: 20px; }
#live-feed-wrapper { overflow-y: hidden; }
#live-feed-snippet{
    width: 100%;
    overflow: scroll;
    white-space: nowrap;
    -webkit-overflow-scrolling: touch;
    display: inline-block;
    vertical-align: middle;
    margin: 0 5px;
    -ms-overflow-style: none;
} 
#live-feed-snippet div {
    display: inline-block;
    margin-right: 1%;
    width: 200px;
    vertical-align: middle;
}
#live-feed-wrapper .left-arrow {
    position: absolute;
    left: 20px;
    margin-top: 95px;
}
#live-feed-wrapper .right-arrow {
    position: absolute;
    right: 20px;
    margin-top: -150px;
}
#live-feed-wrapper img {
  width: 200px;
  height: 200px;
}
/* footer */
footer { padding: 30px; }
.title-border { 
  height: 1px;
  width: 30px;
  background-color: #ffc13d;
  margin-bottom: 12pt;
 }
 b { font-weight: bolder; }

footer .third:not(:nth-of-type(3n)) {
  margin-right: 30px;
}
footer input{
    background: transparent;
    line-height: 2;
    border: none;
    padding: 0 5px;
    color: white;
}
footer form { width: fit-content; }

footer ::placeholder { color: white; }
/* index page*/
#atf { margin-bottom: 20px; }
.spacer-top { margin-top: 20px; }
#product-detail-image {
  background-image: url({{'placeholder.png' | asset_url}});
  background-size: cover;
}
#catalog-cta { width: 400px; }
#featured-catalog {
  margin-bottom: 20px;
  background-color: gray;
  background-size: cover;
  min-height: 350px;
}
#featured-products .flex-container-no div:first-of-type {
  margin-left: 20px;
}
#featured-products .flex-container-no div {
  margin-right: 20px;
}
